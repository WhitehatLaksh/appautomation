# maestro/flows/delete_user_flow.yaml
appId: "com.example.yourapp"
flows:
  - name: "Delete User Flow"
    steps:
      - launchApp
      - tapOn: "Settings"
      - tapOn: "Delete Account"
      - confirm
      - waitForVisible: "Account Deleted"
